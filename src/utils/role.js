import { useEffect } from "react"
import { useSelector } from "react-redux"


  const Token = localStorage.getItem('Token')
  const role_of_user = useSelector((state)=>state.role)
  useEffect(()=>{
    if(Token){
        if(role_of_user = spueruser){
            
        }

    }
  },[])